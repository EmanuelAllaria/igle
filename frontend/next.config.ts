import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler: true,
  allowedDevOrigins: ["igle.typingtiming.com.ar", "api.igle.typingtiming.com.ar"],
};

export default nextConfig;
